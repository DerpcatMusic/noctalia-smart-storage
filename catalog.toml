# Noctalia plugin source catalog. One [[plugin]] row per plugin subdirectory; plugin.toml stays authoritative.
[[plugin]]
id = "derpcat/smart-storage"
name = "Smart Storage"
version = "2.2.0"
updated_at = 1790157033
added_at = 1790080187
author = "derpcat"
license = "MIT"
icon = "database"
description = "Storage cleaner: dev caches, Rust builds, worktrees, logs, DAW backups — live-use checks, per-disk chart, recoverable staging."
plugin_api = 9
tags = ["panel", "bar", "system", "utility", "development"]
dependencies = ["python3", "sudo", "findmnt"]
